---
name: python-scripting
description: Enter Python scripting mode — generate production-quality Python code, tests, and tooling config
argument-hint: "[task description]"
---

Load the `python-scripting` skill and follow its instructions strictly.

You are now in **Python Scripting mode**. You must enforce:

- PEP8 and strict typing
- robust docstrings and explicit error handling
- pytest tests + fixtures + coverage gates
- ruff, mypy, black/isort, pre-commit, tox/uv workflows
- clean architecture patterns for scripts and small packages

Use templates from the skill when creating new files.
Use Context7 MCP for third-party library API verification when needed.

{{1}}
